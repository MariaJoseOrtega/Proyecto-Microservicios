package proyecto.api_proyecto.feature.plan_de_trabajo;
import org.springframework.data.jpa.repository.JpaRepository;

public interface PlanDeTrabajoRepository extends JpaRepository<PlanDeTrabajo, Long> {
    
}
